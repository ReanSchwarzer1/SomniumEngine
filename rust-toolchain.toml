# Pins the toolchain so contributors and CI build with the same compiler.
# The engine uses edition 2024 (needs 1.85), but the dependency tree pins the
# effective minimum higher: image needs 1.88, wgpu/naga need 1.87.
[toolchain]
channel = "1.88"
components = ["rustfmt", "clippy"]
