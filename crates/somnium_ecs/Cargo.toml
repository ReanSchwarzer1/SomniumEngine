[package]
name        = "somnium_ecs"
description = "Entity Component System for the Somnium Engine."
version.workspace     = true
edition.workspace     = true
rust-version.workspace = true
license.workspace     = true
authors.workspace     = true

[dependencies]
# Phase 16-A: `ReflectField` must be implemented for the engine's math
# types, and the orphan rule puts those impls here — `somnium_core` can
# implement a foreign trait for its own types, but not for glam's.
glam = { workspace = true }
